import Link from "next/link";
import { getAdminPageSession } from "@/lib/admin";
import { getDestinationsForAdmin } from "@/lib/destinations";
import { localizeField, resolveLocale, withLocale } from "@/lib/i18n";

export const dynamic = "force-dynamic";
export default async function AdminDestinationsPage({ searchParams }: { searchParams: Promise<{ lang?: string }> }) { await getAdminPageSession(); const { lang } = await searchParams; const locale = resolveLocale(lang); const destinations: any[] = await getDestinationsForAdmin(); return <main dir={locale === "ar" ? "rtl" : "ltr"} className="page-shell space-y-8 pb-20"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.25em] text-clay">Content</p><h1 className="mt-2 text-4xl font-black">Destinations</h1><p className="mt-2 text-sm text-ink/55">Manage multilingual Morocco travel guides, publishing and ordering.</p></div><Link href={withLocale("/admin/destinations/new", locale)} className="rounded-xl bg-forest px-5 py-3 text-sm font-black text-white">Create destination</Link></div><div className="overflow-hidden rounded-[2rem] border border-ink/10 bg-white shadow-card"><div className="divide-y divide-ink/10">{destinations.map((item) => <div key={item._id} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-black">{localizeField(item.name, locale)}</h2><p className="mt-1 text-xs text-ink/45">/{item.slug} · {item.published ? "Published" : "Draft"} · order {item.displayOrder}</p></div><Link href={withLocale(`/admin/destinations/${item._id}`, locale)} className="rounded-xl border border-ink/10 px-4 py-2 text-sm font-bold">Edit</Link></div>)}{!destinations.length && <div className="p-10 text-center text-ink/50">No destinations yet.</div>}</div></div></main>; }
