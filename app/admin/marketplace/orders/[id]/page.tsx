import Link from "next/link";
import { getResellingOrdersForAdmin } from "@/lib/reselling-marketplace";
import { OrderStatusControl } from "@/components/order-status-control";
export const dynamic="force-dynamic";
export default async function OrderDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const orders=await getResellingOrdersForAdmin();const order=orders.find(o=>String(o._id)===id);if(!order)return <main><p>Order not found.</p></main>;const title=order.productSnapshot?.title?.en||order.productSnapshot?.title?.fr||order.productSnapshot?.title?.ar||"Product";const options=order.selectedOptions&&typeof order.selectedOptions==="object"?Object.entries(order.selectedOptions):[];return <main className="space-y-6"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Order</p><h1 className="mt-2 text-3xl font-black">{order.orderNumber}</h1></div><OrderStatusControl id={order._id} status={order.status}/></div><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-2xl bg-white p-6 shadow-card"><h2 className="text-xl font-black">Customer</h2><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between gap-4"><dt>Name</dt><dd className="font-bold">{order.customerName}</dd></div><div className="flex justify-between gap-4"><dt>Phone</dt><dd><a className="font-bold text-emerald-700" href={`tel:${order.phone}`}>{order.phone}</a></dd></div><div className="flex justify-between gap-4"><dt>City</dt><dd className="font-bold">{order.city}</dd></div><div><dt>Address</dt><dd className="mt-1 font-bold">{order.address}</dd></div><div><dt>Customer note</dt><dd className="mt-1 text-slate-600">{order.customerNote||"—"}</dd></div></dl></section><section className="rounded-2xl bg-white p-6 shadow-card"><h2 className="text-xl font-black">Order</h2><dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between gap-4"><dt>Product</dt><dd className="font-bold">{title}</dd></div>{options.map(([key,value])=><div key={key as string} className="flex justify-between gap-4"><dt>{key as string}</dt><dd className="font-bold">{String(value)}</dd></div>)}<div className="flex justify-between gap-4"><dt>Variant ID</dt><dd className="font-bold break-all">{order.variantId||"—"}</dd></div><div className="flex justify-between gap-4"><dt>Quantity</dt><dd className="font-bold">{order.quantity}</dd></div><div className="flex justify-between gap-4"><dt>Unit price</dt><dd className="font-bold">{order.unitPrice} DH</dd></div><div className="flex justify-between gap-4"><dt>Total</dt><dd className="text-xl font-black">{order.totalAmount} DH</dd></div><div className="flex justify-between gap-4"><dt>Source cost snapshot</dt><dd>{order.sourceCostSnapshot} DH</dd></div><div className="flex justify-between gap-4"><dt>Expected profit</dt><dd className="font-bold text-emerald-700">{order.profitSnapshot} DH</dd></div><div className="flex justify-between gap-4"><dt>Payment</dt><dd className="font-bold">Cash on Delivery</dd></div></dl></section></div><div className="rounded-2xl bg-white p-6 shadow-card"><h2 className="text-xl font-black">Variant snapshot</h2><p className="mt-2 text-sm text-slate-500">Historical copy of the selected source variant; it remains available even if the product is later synchronized.</p><pre className="mt-4 overflow-auto rounded-xl bg-slate-50 p-4 text-xs">{JSON.stringify(order.variantSnapshot||{},null,2)}</pre><div className="mt-5 flex flex-wrap gap-3"><Link href="/admin/marketplace/orders" className="rounded-xl border px-5 py-3 font-bold">Back to orders</Link><a href={`tel:${order.phone}`} className="rounded-xl bg-[#0f3d2e] px-5 py-3 font-bold text-white">Call customer</a></div></div></main>}
