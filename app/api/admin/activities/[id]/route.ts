import { NextResponse } from "next/server"; import { getAdminApiSession } from "@/lib/admin"; import { validateActivityPayload } from "@/lib/activity"; import { connectToDatabase } from "@/lib/db"; import Activity from "@/models/Activity";
type Context = { params: Promise<{ id: string }> };
export async function PATCH(request: Request, context: Context) { const admin = await getAdminApiSession(); if ("error" in admin) return admin.error; try { const validation = validateActivityPayload(await request.json()); if ("error" in validation) return NextResponse.json({ error: validation.error }, { status: 400 }); await connectToDatabase(); const { id } = await context.params; if (await Activity.exists({ slug: validation.data.slug, _id: { $ne: id } })) return NextResponse.json({ error: "That activity slug is already in use." }, { status: 409 }); const activity = await Activity.findByIdAndUpdate(id, validation.data, { new: true, runValidators: true }); if (!activity) return NextResponse.json({ error: "Activity not found." }, { status: 404 }); return NextResponse.json({ activity }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not update activity." }, { status: 500 }); } }
export async function DELETE(_request: Request, context: Context) { const admin = await getAdminApiSession(); if ("error" in admin) return admin.error; try { await connectToDatabase(); const { id } = await context.params; const activity = await Activity.findByIdAndDelete(id); if (!activity) return NextResponse.json({ error: "Activity not found." }, { status: 404 }); return NextResponse.json({ success: true }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not delete activity." }, { status: 500 }); } }
