import { NextResponse } from "next/server"; import { connectToDatabase } from "@/lib/db"; import AffiliateProduct from "@/models/AffiliateProduct"; type Context = { params: Promise<{ id: string }> }; export async function POST(request: Request, context: Context) { try { const { event } = await request.json() as { event?: string }; const field = event === "view" ? "viewCount" : event === "click" ? "clickCount" : null; if (!field) return NextResponse.json({ error: "Unsupported analytics event." }, { status: 400 }); await connectToDatabase(); const { id } = await context.params; const product = await AffiliateProduct.findOneAndUpdate({ _id: id, status: "published" }, { $inc: { [field]: 1 } }); if (!product) return NextResponse.json({ error: "Product not found." }, { status: 404 }); return NextResponse.json({ success: true }); } catch { return NextResponse.json({ error: "Could not record product analytics." }, { status: 400 }); } }
