"use client";
import Image from "next/image";
import Link from "next/link";
import { useState } from "react";
import { withLocale, type SiteLocale } from "@/lib/i18n";
export function ResellingProductCard({ product, locale }: { product: any; locale: SiteLocale }) { const [busy,setBusy]=useState(false); const title=product.title?.[locale]||product.title?.fr||product.title?.en; const desc=product.shortDescription?.[locale]||product.shortDescription?.fr||product.description?.en; return <article className="group overflow-hidden rounded-[2rem] border border-slate-100 bg-white shadow-[0_20px_60px_rgba(15,61,46,.08)]"><Link href={withLocale(`/marketplace/${product.slug}`,locale)}><div className="relative h-72 bg-slate-50"><Image src={product.mainImage} alt={title} fill sizes="(max-width:768px) 100vw, 33vw" className="object-contain p-6 transition duration-500 group-hover:scale-105" />{product.badges?.length ? <div className="absolute left-4 top-4 flex gap-2"><span className="rounded-full bg-white px-3 py-1 text-xs font-bold shadow">{product.badges[0]}</span></div>:null}</div></Link><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">{product.category||"Marketplace"}</p><h2 className="mt-2 text-xl font-black text-slate-900">{title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-500">{desc}</p><div className="mt-5 flex items-center justify-between gap-3"><strong className="text-2xl font-black text-slate-900">{product.sellingPrice} DH</strong><Link onClick={()=>setBusy(true)} href={withLocale(`/marketplace/${product.slug}`,locale)} className="rounded-full bg-[#0f3d2e] px-5 py-3 text-sm font-black text-white">{busy?"...":locale==="ar"?"اطلب الآن":locale==="fr"?"Commander":"Order now"}</Link></div></div></article> }
