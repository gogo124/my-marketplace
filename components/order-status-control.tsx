"use client";
import { useState } from "react";
export function OrderStatusControl({id,status}:{id:string;status:string}){const [value,setValue]=useState(status);const [busy,setBusy]=useState(false);async function change(next:string){setValue(next);setBusy(true);await fetch(`/api/admin/marketplace/orders/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:next})});setBusy(false)}return <select disabled={busy} value={value} onChange={e=>change(e.target.value)} className="rounded-lg border px-2 py-2 text-xs font-bold"><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="processing">Processing</option><option value="shipped">Shipped</option><option value="delivered">Delivered</option><option value="cancelled">Cancelled</option><option value="customer_unreachable">Customer unreachable</option></select>}
