import { Award, BadgeCheck, Box, Clock3, Leaf, PackageCheck, RotateCcw, ShieldCheck, Sparkles, Star, Truck, Zap } from "lucide-react";
const icons: Record<string, typeof Star> = { "Free Shipping": Truck, "Fast Delivery": Zap, "Express Delivery": Clock3, "Easy Returns": RotateCcw, "Warranty Included": ShieldCheck, "Secure Purchase": ShieldCheck, "Verified Product": BadgeCheck, "Premium Quality": Award, "Eco Friendly": Leaf, "Official Store": PackageCheck, "Money Back Guarantee": ShieldCheck };
export function ProductBadge({ label }: { label: string }) { return <span className="inline-flex items-center gap-1.5 rounded-full border border-orange-200 bg-orange-50 px-2.5 py-1 text-[11px] font-black uppercase tracking-wide text-orange-800"><Sparkles className="h-3 w-3" />{label}</span>; }
export function ProductFeature({ label, compact = false }: { label: string; compact?: boolean }) { const Icon = icons[label] || Box; return <span className={`inline-flex items-center gap-2 rounded-xl border border-emerald-100 bg-emerald-50 font-semibold text-emerald-900 ${compact ? "px-2.5 py-1.5 text-xs" : "px-3 py-2 text-sm"}`}><Icon className={compact ? "h-3.5 w-3.5" : "h-4 w-4"} />{label}</span>; }
