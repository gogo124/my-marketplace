"use client";
import Image from "next/image";
import { useState } from "react";
import { ImageLightbox } from "@/components/image-lightbox";
export function ProductGallery({ images, alt, fit = "contain" }: { images: string[]; alt: string; fit?: "contain" | "cover" }) { const gallery = Array.from(new Set(images.filter(Boolean))); const [index, setIndex] = useState(0); const [open, setOpen] = useState(false); const previous = () => setIndex((current) => (current - 1 + gallery.length) % gallery.length); const next = () => setIndex((current) => (current + 1) % gallery.length); return <div className="space-y-4"><div className="relative h-[460px] overflow-hidden rounded-[2.75rem] bg-white shadow-[0_30px_85px_rgba(15,61,46,.14)] sm:h-[620px]"><button type="button" onClick={() => setOpen(true)} className="absolute inset-0"><Image src={gallery[index]} alt={alt} fill priority sizes="(max-width: 1024px) 100vw, 60vw" className={fit === "cover" ? "object-cover transition duration-500 hover:scale-[1.02]" : "object-contain p-5 transition duration-500 hover:scale-[1.02] sm:p-8"} /></button><div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/15 via-transparent to-transparent" />{gallery.length > 1 ? <><button type="button" onClick={previous} className="absolute left-4 top-1/2 z-10 -translate-y-1/2 rounded-full bg-white/90 px-5 py-3.5 text-xl font-black shadow-lg backdrop-blur">‹</button><button type="button" onClick={next} className="absolute right-4 top-1/2 z-10 -translate-y-1/2 rounded-full bg-white/90 px-5 py-3.5 text-xl font-black shadow-lg backdrop-blur">›</button><span className="absolute bottom-4 right-4 rounded-full bg-black/55 px-4 py-2 text-xs font-black text-white backdrop-blur">{index + 1} / {gallery.length}</span></> : null}</div>{gallery.length > 1 ? <div className="flex gap-3 overflow-x-auto pb-2">{gallery.map((image, itemIndex) => <button key={image} type="button" onClick={() => setIndex(itemIndex)} className={`relative h-24 w-32 shrink-0 overflow-hidden rounded-[1.25rem] border-2 bg-white shadow-sm transition ${index === itemIndex ? "border-clay" : "border-transparent opacity-70 hover:opacity-100"}`}><Image src={image} alt={`${alt} ${itemIndex + 1}`} fill sizes="128px" className={fit === "cover" ? "object-cover" : "object-contain p-2"} /></button>)}</div> : null}{open ? <ImageLightbox images={gallery} alt={alt} index={index} onClose={() => setOpen(false)} /> : null}</div>; }
