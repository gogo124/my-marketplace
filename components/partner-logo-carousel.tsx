import Image from "next/image";
type Partner = { _id: string; name: string; logoUrl: string; websiteUrl?: string };
export function PartnerLogoCarousel({ partners }: { partners: Partner[] }) { if (!partners.length) return null; const items = partners.length === 1 ? [...partners, ...partners, ...partners, ...partners] : [...partners, ...partners]; return <div className="partner-carousel mt-10 overflow-hidden rounded-[2rem] border border-white/10 bg-white/5 py-6 backdrop-blur" aria-label="Trusted affiliate partners"><div className="partner-carousel-track flex w-max items-center gap-5 px-5">{items.map((partner, index) => { const logo = <div className="relative h-24 w-44 overflow-hidden rounded-[1.5rem] bg-white p-4 shadow-[0_16px_35px_rgba(0,0,0,.12)] sm:h-28 sm:w-52"><Image src={partner.logoUrl} alt={`${partner.name} logo`} fill sizes="208px" className="object-contain p-5" /></div>; return partner.websiteUrl ? <a key={`${partner._id}-${index}`} href={partner.websiteUrl} target="_blank" rel="noopener noreferrer sponsored" title={partner.name} className="shrink-0 transition hover:-translate-y-1">{logo}</a> : <div key={`${partner._id}-${index}`} title={partner.name} className="shrink-0">{logo}</div>; })}</div></div>; }
